import { render, screen } from '@testing-library/react'
import HtmlBodyTags from '../../../components/htmlTags/HtmlBodyTags'

describe('NavLink component', () => {
  test('Renders nav items we send to it', () => {
    // render(<HtmlBodyTags />)
  })
})
